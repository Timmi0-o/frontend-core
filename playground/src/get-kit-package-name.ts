/**
 * Путь для копируемых примеров.
 * Внешний вид задаёт приложение (CSS кита + `data-ui-kit`), не суффикс импорта.
 */
export const getKitPackageName = (_kitId: string): string => {
	return '@timmi0-o/frontend-core/ui-kit'
}
